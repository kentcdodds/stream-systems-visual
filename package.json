{
  "name": "stream-systems-visual",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "capture-thumbnails": "npm run build && node scripts/capture-thumbnails.ts",
    "capture-thumbnails:only": "node scripts/capture-thumbnails.ts",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "playwright": "^1.52.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^4.14.0"
  }
}
